
#include "wss/type.hpp"
#include "wss/i_dependency_reference.hpp"
#include "wss/dependency_set.hpp"

/*
class i_named_formula_reference
            : public i_dependency_reference
{
public:
    virtual ~i_named_formula_reference() {}

    virtual reference_type type() const = 0;

    virtual sheet_range position() const = 0;

    virtual const std::string& name() const = 0;

    virtual locked_dependency_set_type * operator ->() = 0;

// virtual static shared_reference_type create( shared_dependency_type& dependent, i_named_formula_manager& nfm, const shared_dependency_type& sld  ) = 0;
};*/
